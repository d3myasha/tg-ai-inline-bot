services:
  bot:
    image: ghcr.io/d3myasha/tg-ai-inline-bot:latest
    pull_policy: always
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot-data:/data

volumes:
  bot-data: